# 服务监听配置
HOST=127.0.0.1
PORT=8000

# SQLite 数据库文件路径(相对项目根目录)
DB_PATH=weights.db

# 日志文件路径
LOG_PATH=logs/app.log

# 移动平均计算窗口(逗号分隔, 单位: 天)
MA_WINDOWS=3,7

# 是否开启热重载(开发用 true, 后台运行保持 false)
RELOAD=false
